시간복잡도 `O(N)`
> Checkpoint : 1순위 상원이 친구, 2순위 상원이 친구의 친구
>
> 상원이는 1번<br>
> 1번부터 트리처럼 뻗어나간다고 생각햐면 됨
> 

트리 그릴 때 배열로그리는데 vector쓰면 편함<br>
벡터 배열로 만들어서 부모노드를 배열로 접근하고 자식 노드는 vector로 동적으로 조절하는 것

만약 이진트리다? 그러면 그냥 이차원배열로 쓸 듯`[N][2]`

c접근도 `for(idx: graph[i])`로 꺼내면 자식 노드 개수를 몰라도 됨<br>
물론 알고 접근해도 무방함. 

후기: 상원이는 빼고 계산해야하는걸 잊지말자. 초대된사람 셀 때 bool 배열로 만들었는데<br>
unordered_set을 쓰려고했음. 그냥 중복들어가도 알아서 저장 안될테니까. 이걸 써도 무방하지만<br>
지금 입력값이 많아봤자 500개 쓰는거니까 배열을 써도 됨. <br>
입력값이 너무 많을때 unordered_set
